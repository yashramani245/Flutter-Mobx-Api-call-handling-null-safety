name: mobx_demo
description: mobx_demo


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  mobx_codegen: ^2.0.1+3
  cupertino_icons: ^1.0.3
  mobx: ^2.0.1
  flutter_mobx: ^2.0.0
  url_launcher: ^6.0.4
  http: ^0.13.3

dev_dependencies:
  build_runner: ^2.0.3

 # flutter_test:
  #  sdk: flutter
  #integration_test:
    #sdk: flutter



flutter:

  uses-material-design: true

